@model List<Product>

<div class="container mb-2">
    <div class="row pt-4">
        <div class="col-4 d-flex justify-content-start">
            <h6 class="text-primary fw-lighter">
                Filter by...
            </h6>
        </div>
        <div class="col-4 d-flex justify-content-center">
            <h3 class="text-primary" style="font-weight:bold">
                Books collection

            </h3>
        </div>
        <div class="col-4 d-flex justify-content-end">
            <a asp-controller="Product" asp-action="Upsert" class="btn btn-primary">
                Add new
            </a>
        </div>
    </div>
</div>

<table id="tblData" class="table table-striped">
    <thead>
        <tr>
            <th>
                Name
            </th>
            <th>
                Author
            </th>
            <th>
                Description
            </th>
            <th>
                Publish year
            </th>
            <th>

            </th>
        </tr>
    </thead>
</table>

    @* @foreach(var product in Model)
    {
        <div class="d-flex flex-row mt-2 mb-1" style="height:21.5em; background-color:lightgreen; border-radius:10px; margin-left:1em">
            <div style="height:16.5em; width:13.5em; margin-left:1em" class="d-flex flex-wrap justify-content-center">
                <div class="d-flex flex-column align-items-center justify-content-center">
                    <h4>@product.Name</h4>
                    <h6>@product.Author - @product.PublishDate.Year</h6>

                </div>
                <div style="height:14em; width: 13.5em;">
                    @{
                        if(product.Image != null)
                        {
                            var imreBase64Data = product.Image;
                            byte[] bytes = Convert.FromBase64String(imreBase64Data);
                            var image = "data:image/png;base64," + Convert.ToBase64String(bytes, 0, bytes.Length);
                            <img src="@image" style="width:inherit; height:inherit;" />
                        }
                        else
                        {
                            <img src="" alt="Book image" />
                        }
                    }
                </div>
                <div class="d-flex flex-row align-items-center justify-content-center w-100 mt-2">
                    <div class="d-flex flex-row justify-content-center align-items-center w-100">
                        
                        <div class="d-flex flex-row w-100 align-items-center flex-wrap">
                            <button class="btn btn-primary" style="margin-right:0.5em; font-size:0.75em">
                                <svg xmlns="http://www.w3.org/2000/svg" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="#ffffff" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><line x1="12" y1="1" x2="12" y2="23"></line><path d="M17 5H9.5a3.5 3.5 0 0 0 0 7h5a3.5 3.5 0 0 1 0 7H6"></path></svg>
                                Buy
                            </button>
                            <button class="btn btn-primary" style="font-size:0.75em">
                                <svg xmlns="http://www.w3.org/2000/svg" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="#ffffff" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="10" cy="20.5" r="1" /><circle cx="18" cy="20.5" r="1" /><path d="M2.5 2.5h3l2.7 12.4a2 2 0 0 0 2 1.6h7.7a2 2 0 0 0 2-1.6l1.6-8.4H7.1" /></svg>
                                Cart
                            </button>
                            <div class="d-flex justify-content-end align-items-center" style="margin-left:0.5em; flex:1">
                                <h6>$@product.Price</h6>
                            </div>
                        </div>                     
                    </div>                                    
                </div>
            </div>
            <div style="width:15.5em;" class="d-flex align-items-center flex-column">
                <div style="height:19em">
                    <p>@product.Description</p>
                </div>
                <div>
                    <a asp-controller="Product" asp-action="Upsert" asp-route-productId="@product.Id">Edit</a>
                    <a asp-controller="Product" asp-action="Delete" asp-route-productId="@product.Id">Delete</a>
                </div>
            </div>
        </div>      
    } *@

@section Scripts{
    <script src="~/js/product.js"></script>

}
